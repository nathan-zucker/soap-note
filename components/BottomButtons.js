import { View, Text, Button } from "react-native";

export default function BottomButtons({navigation}) {
    return (
        <View>
            <Button title="new Patient" />
        </View>
    )
}